{
  "_README": "SAMPLE ONLY — this file is NOT bundled into the app. It shows the shape of a Google Cloud Console 'Desktop app' OAuth client. Create the real client (see win_app/README.md → 'Google OAuth setup'), download its JSON, and distribute that file to each user, who loads it once via Settings → Google sign-in. auth.py also accepts a 'web' client.",
  "installed": {
    "client_id": "REPLACE_ME.apps.googleusercontent.com",
    "client_secret": "REPLACE_ME",
    "project_id": "REPLACE_ME",
    "auth_uri": "https://accounts.google.com/o/oauth2/auth",
    "token_uri": "https://oauth2.googleapis.com/token",
    "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
    "redirect_uris": ["http://localhost:8000/api/auth/callback"]
  }
}
